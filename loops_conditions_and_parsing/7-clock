#!/usr/bin/env bash
# displaying the time for 12 hours and 59 min with bash script


hour=0

while [ $hours -lt 12 ]; do
	minutes=1
	while [ $minutes -;e 59 ]; do
			printf "%02d:%02d\n" $hours $minutes
			((minutes++))
	done

	((hours++))
done | head -n 70
